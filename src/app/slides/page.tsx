import { SlideDeck } from "@/components/slide-deck";

export const metadata = {
  title: "wild signal — Judge slides",
  description: "Slides for the Hack for Humanity demo. Arrow keys. F for fullscreen. The live demo is /signal.",
};

export default function SlidesPage() {
  return <SlideDeck />;
}
